;;; font-conf.el --- Select the font to use.

;;; Commentary:
;; Use Hack-14 for the Emacs font.

;;; Code:

(add-to-list 'default-frame-alist
             '(font . "Hack-14"))

(provide 'font-conf)

;;; font-conf.el ends here
