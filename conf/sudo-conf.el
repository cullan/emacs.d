;;; sudo-conf.el --- Install sudo-edit

;;; Commentary:
;; This package lets you switch to superuse while editing a file.
;; Just do M-x sudo-edit and bask in the power.

;;; Code:

(use-package sudo-edit
  :ensure t)

(provide 'sudo-conf)

;;; sudo-conf.el ends here
