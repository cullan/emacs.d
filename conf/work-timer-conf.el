;;; work-timer-conf.el --- Configure the work-timer package

;;; Commentary:
;; Work timer provides a function that will notify you to take a break
;; after a specified interval has passed.

;;; Code:

(use-package work-timer)

(provide 'work-timer-conf)

;;; work-timer-conf.el ends here
